/* this file contains the mock profile data that would usually derive from a server */
import { Profile } from '../profile-info/profile.model';

export const profileRecord: Profile = { name: 'John', surname: 'Doe', age: 45 };
