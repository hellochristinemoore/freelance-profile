/* this file contains the mock profile data that would usually derive from a server */

import { Profile } from './profile.model';

export const profiles: Profile[] = [
    { name: 'John', surname: 'Doe', age: 45}
]